spring:
  application:
    name: ipl
  data:
    redis:
      host: localhost
      port: 6379
  cache:
    type: redis

  datasource:
    url: jdbc:mysql://localhost:3306/ipl
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
server:
  port: 9000
